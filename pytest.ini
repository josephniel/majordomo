[pytest]
pythonpath = src
asyncio_mode = auto
testpaths = tests
# Integration tests hit the local Postgres (127.0.0.1:5433, the compose
# stack). Override with TEST_DATABASE_URL if it lives elsewhere.
markers =
    integration: touches the live Postgres instance
